#include <stdio.h>

/**
 * main - Prints a string
 * Description: Prints a string with space at the end
 * Return: A string an a 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
};
